# Python intro

## HW 23 

### Update class Club from CW#23 - Method getPersonsByAge(minAge:int, maxAge:int) returns list of all persons with minAge<=age<=maxAge

##### Unit tests
Unit tests for all methods of the class Club in a separate file (module)

### Class Dictionary with following methods:
#### Method addWord(word:str) adds word, duplications based on case insensitive comparing are disallowed

#### Method getWordsByPrefix(prefix: str) returning list of all words starting with the given prefix, comparing based on case insensitive manner but words should keep their case

##### Unit tests for all methods of the class Dictionary